name: dart_prefix_renamer
description: Prefix Dart files and classes while rewriting project-wide references.
version: 0.1.0

environment:
  sdk: ^3.9.2

# Add regular dependencies here.
dependencies:
  analyzer: ^10.0.0
  archive: ^3.6.1
  args: ^2.7.0
  crypto: ^3.0.6
  encrypt: ^5.0.3
  path: ^1.9.0
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
