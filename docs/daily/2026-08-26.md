# 日报 · 2026-08-26

## iOS 第三方 SDK Pod 候选池扩容

- 使用 GitHub 连接器与 `agent-reach` 的 GitHub 路由审核新增候选；Yams 因当前仓库没有 Podspec 而拒绝，采用 Swinject。
- 候选池由 10 扩到 15：新增 SwiftyJSON 5.0.2、ObjectMapper 4.4.2、DifferenceKit 1.3.0、SwiftSoup 2.11.3、Swinject 2.9.1；每项均记录官方仓库、Podspec、许可、最低 iOS 和 Privacy Manifest 状态。
- 新增 Swift probe 仅处理固定内存输入：JSON 读取、Map 构造、数组差分、HTML 解析和依赖容器解析；没有网络、权限、持久化、UI 修改、Flutter 回传、日志或分析调用。
- 确定性选择数量由固定 3 改为严格的 1–10。图片候选 Kingfisher/SDWebImage 仍互斥，15 个候选最多可同时选出 14 个，但产品配置上限按需求固定为 10。
- 环信 `config/dependencies.yaml` 已设置 `count: 7`。`product_abc + seed 0` 的选择为 SwiftSoup、Swinject、DifferenceKit、SwiftyJSON、Kingfisher、GRDB.swift、PromiseKit。
- 修复实测：CocoaPods trunk 可解析 Swinject 2.9.1，但不存在此前写入的 2.10.0；已固定为 2.9.1。安装用户级 Ruby `plist` 3.7.2 后，`fluwx` 的 `wechat_setup.rb` 可正常加载，现有输出副本 `pod install` 成功，第三方报告为 `selected=declared=locked=called=7`、`passed=true`。
- 修复提交：工具 `879659b`；根脚本 `b1657ea`。
- DifferenceKit 编译修复：probe 的成员初始化器统一使用 `value:` 标签；生成器现在会检测模板漂移并刷新现有本地 Pod。实测无签名 iPhoneOS Release 构建成功，unsigned IPA 的 Inspector 最终为 `selected=declared=locked=called=linked=7`、`passed=true`。工具提交：`8084c93`；IPA 不提交 Git。
- 验证：格式检查、29 项专项依赖测试与空白检查通过；Analyzer 无 error/warning（39 条既有 info）。未触碰已有输出副本，未重新构建 7 库 IPA。
- 工具提交：`0c72293`（`功能：扩容 iOS 第三方 Pod 候选池 / feat: expand iOS third-party Pod pool`）；根项目文档提交待本次提交后补充。
