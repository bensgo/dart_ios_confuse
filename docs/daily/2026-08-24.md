# 日报 · 2026-08-24

## Release 差异化工程连续实施

- 完成 WP-06～WP-10 基础实现：六类 Native Capability、Pigeon contract、依赖检查、Release Inspector、独立 capability pipeline 和报告。
- 完成 WP-11 基础项：README、CHANGELOG、CI Release Gate 骨架；明确 legacy `--junk-code` 不进入能力报告。
- 修复恢复阶段扫描随机 junk 目录造成的性能问题。
- 验证：Analyzer 无 error/warning；8 项新增专项测试及完整 63 项测试通过。
- 工具提交：`7a25d54`（`功能：接入能力差异化流水线 / feat: integrate capability differentiation pipeline`）。
- 当前门禁：真实环信重命名与 Release 验证必须等待用户提供本次小写英文前缀；未声称已完成 IPA、Swift 编译或运行验证。

## 环信真实工程验证（前缀 abc）

- 第一轮输出：`huanxin_all_output`；源工程保持干净。
- 重命名统计：1338 个 Dart 文件、298 个目录、1854 个 Class、927 个 Asset；URI 7082 处、Class 35093 处、Asset 路径 973 处。
- 门禁失败：Analyzer `302 -> 6284`，新增 5982 个错误，因此未进入主题模块阶段。
- 根因：旧 `lib/pack/junk_code` 被排除语义扫描，但仍被文件重命名，导致其数百个相对 import 失效。
- 修复：文件重命名阶段同样完整跳过旧 junk-code 文件图，并增加回归测试；提交 `e55878f`。
- 验证：新增回归测试通过；Analyzer 无 error/warning。失败输出保留，等待用户选择归档或改用新输出路径后重跑。

## 环信主题化输出完成

- 经用户授权将第一轮失败输出移入 macOS 废纸篓，并使用前缀 `abc` 重跑。
- 第一阶段：1081 个 Dart 文件、298 个目录、1854 个 Class、927 个 Asset；Analyzer `302 -> 302`。
- 第二阶段主题：离线会话展示排期模拟；生成 10 个 Dart 文件、10 个 Class，分布于 model/catalog/strategy/coordinator 四个子目录。
- 入口：`lib/main.dart` 调用一次 `JunkCodeMain()`，随后保持调用一次重命名 App 入口。
- 旧 junk 备份：`huanxin_all_output/dart_prefix_renamer_theme_backup/junk_code_20260824_1017`。
- 第二阶段 Analyzer：`302 -> 302`，无新增错误。
- 最终元数据：重置 6911 个实体，时间 `2026-08-24T10:19:31`。
- 未执行 Flutter Release 构建或设备启动，因此不声明 App 可运行。

## Native/Pigeon 深度集成

- WP-06 增加 NativeCapabilities 本地 podspec 与 Runner Podfile 幂等接线，修正 Performance 动态标记 Swift API。
- Pigeon 由 pub 求解器锁定为兼容 Dart 3.9/Analyzer 9 的 `26.3.2`。
- 当前异常：`dart run pigeon` 对最小 schema 持续占用 CPU 且无输出；已确认 CLI 本身可启动，需继续定位 parser/generator 兼容问题后才能完成 WP-07。
- 工具提交：`9148fc7`。
- 已定位卡死来自 Pigeon 在工具根 package 中建立 Analyzer 上下文；改为独立 runner 后成功生成。
- Pigeon 固定为 `26.3.4`，生成步骤带 30 秒超时、非零失败关闭和输出路径验证。
- Analyzer 10.2.0 下完整 64 项测试通过；工具提交 `f65a86a`。
- 新增 Pigeon Swift host handler、AppDelegate 幂等注册和 NativeCapabilityBridge；专项测试通过，工具提交 `f371617`。
- 增加 iPhoneOS Swift 编译门禁并修复真实命名冲突；跨模块 Pigeon/Native handler 类型检查通过，提交 `79dcca7`。
- `--capabilities` 已接入复制与事务两模式，能力统计进入 RenameReport/CLI，提交 `b92f9d9`。
- manifest v2 与 capability 安全 restore 完成；端到端恢复和完整 66 项测试通过，提交 `b867a64`。
- 依赖 declaration/lock/API/IPA linkage 与 IPA 主 bundle 二进制门禁完成，提交 `d9c571e`、`eef0dbc`。
- 环信无签名 Release archive 构建成功（546 MB）；unsigned IPA 检查 `passed`，报告已生成。Flutter 警告版本号缺失和 Launch Image 占位。
- 最终元数据重置 12614 个实体（先为只读 Pods 增加用户写权限）；完整 67 项测试通过。
- 双 Profile CI Gate 已实现，提交 `01d10b3`；等待私有 CI 配置 `RELEASE_FIXTURE_ROOT`/`RELEASE_TARGETS` 后实际执行两份 profile。
- 新增 alpha/beta 双 Product Profile fixture：Native 组合分别为 4/5 个模块，pipeline 均通过且 composition hash 确认不同。
- README 已补齐 `--capabilities` 参数组、报告/seed/manifest 配置和 manifest v2 哈希匹配安全恢复说明。
- 最终回归：`fvm dart analyze` 无 error/warning（38 条既有 info），`fvm dart test` 全部 68 项通过；工具提交 `7dccdd5`。
- WP-07 已完成；WP-11 提升至 95%。剩余仅为依赖私有 Product Profile、签名凭据与私有 Release fixture 的 CI 双构建门禁。
- README 共享功能开关已按六类用途分组，方便按场景查找；仅调整文档结构，未改变任何命令行为；工具提交 `27534b5`。
- 修正模式一/模式二完整示例：统一产品参数和构建产物说明，并移除模式二已失效的产物参数；专项事务构建与 Pod 测试通过；工具提交 `092386d`。
- 扫描环信真实调用链并新增 `config/` 下五份 Capability manifest 初稿：产品 `product_abc`，Native 启用 device/network/crypto，未声明第三方依赖；未发现可安全接入的既有 Dart Capability catalog 调用，因此 Dart integration 保持空。
- 在临时副本执行 Capability pipeline 通过：3947 Classes、Native 3/3 called、0 第三方依赖；临时副本已移至废纸篓。环信仅有未提交配置初稿，未修改业务源码。
- 完成 Product Profile 单一事实来源重构：移除无效 `features` 与重复 `technical`；遗留字段被严格 schema 以 `E002` 拒绝。能力开关现仅由 Dart/Native manifest 管理。
- 验证：新增 schema 回归；专项 25 项、完整 69 项测试通过，Analyzer 无 error/warning（38 条既有 info）；工具提交 `e88bbe8`。

## Dart 候选类分类扩展

- 新增 `utility`、`manager`、`logic` 三种 Analyzer 分类；`Utility`、`Util`、`Helper` 归为 `utility`，并采用 token 匹配避免将 `Reutilization` 误判。
- 新分类可进入候选类统计；`capability-plan.json` 增加完整 `classes` 与 `classification_summary`，含候选资格及不合格原因。
- 新分类尚无 capability 模板，统一输出空 `suggestions`，不触发生产代码生成。
- 专项 5 项测试通过；Analyzer 无 error/warning（38 条既有 info）。完整回归受本地单次命令 30 秒时限影响未取得最终退出码，两轮均运行至 68 项集成测试且未见失败。
- 工具提交：`9a08a0a`（`功能：扩展 Dart 候选类分类 / feat: extend Dart candidate classifications`）。

## Dart 百分比自动能力插入

- 新增只读扫描入口 `--scan-dart-capabilities`，为全部非生成 Class 输出类型、建议、候选方法和不合格原因。
- 新增 `--auto-dart-capabilities`：根据 `--dart-differentiation-percent` 和 seed 确定性随机选取候选，输出含源码 SHA-256 的可复用 selection JSON，并插入真实可达且不改变返回值的调用。
- 提供 `--dart-capability-selection` 时跳过随机；分类、方法、Capability、重复项或源码哈希漂移时失败关闭。
- 真实 CLI fixture：20 个 Class、11 个候选、100% 时选择并插入 11 项；生成源码语法检查通过。
- 验证：专项 11 项、完整 74 项测试通过；Analyzer 无 error/warning（45 条 info）。
- 工具提交：`84d60a1`（`功能：实现 Dart 百分比能力插入 / feat: implement percentage-based Dart capability insertion`）。
- 真实环信全量扫描完成：目标 `lib`、`packages/aixi_foundation/lib`，输出 3942 个 Class、124 个自动插入候选；默认基线文件保留为私有工程 `reports/dart-capability-suggestions.json`（1.2 MB，SHA-256 `b173fdf309212e1b9902d3472d3321d3965af2d5bd41cf7f359154d8a5b364fc`），不提交公共仓库。
- 修复大型 Workspace 扫描长时间无输出：Analyzer 限制到 `--target`，CLI 增加启动与文件解析进度。真实扫描耗时约 66 秒，后续终端可持续确认进度。
- 扫描进度修复提交 `a4bd2cc`；完整 74 项测试通过。
