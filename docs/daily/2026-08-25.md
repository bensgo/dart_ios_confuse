# 日报 · 2026-08-25

## 工具文档拆分

- `dart_prefix_renamer/README.md` 收敛为功能、参数和单项使用说明。
- 新增 `dart_prefix_renamer/USAGE.md`，集中提供完整模式一和模式二命令；模式一包含第三方 SDK Pod 接入和 IPA 验证，模式二明确不包含该独立功能。
- 工具提交：`5f9e082`（`文档：拆分功能与模式用法 / docs: split features and mode usage`）。

## Dart、自定义 Pod 与第三方 SDK Pod 解耦

- 将能力命名拆成三类：Dart 自动能力、iOS 自定义源码 Pod、iOS 第三方 SDK Pod。
- 新增独立 `--prepare-ios-third-party-sdk-pods`；只读取依赖清单和候选池，不再要求 Product Profile、Dart/Native manifest 或 `--target`。
- 自定义 Pod 新命名为 `--ios-custom-pod-*`；旧 `ios-product-*` 保留为隐藏兼容别名。
- 新增兼容删除清单；旧混合 capability 流水线在 CI/私有脚本迁移前不硬删。
- 完整 82 项测试通过；Analyzer 无 error/warning（39 条既有 info）；此前 9 个格式缺口已清理。
- 工具提交：`25a40b8`（`功能：拆分 Dart 与 iOS Pod 能力 / feat: separate Dart and iOS Pod capabilities`）。

## 模式二完整示例纳入 Dart 自动能力插入

- `--transactional-build` 已支持 `--auto-dart-capabilities`：在已重命名的临时副本中完成选择和插入，再执行 IPA 构建；临时副本最终删除，原工程源码不被修改。
- README 的模式二完整示例已加入 50%、seed `0` 和 `--artifact-dir="reports"`，用于带回 suggestions/selection JSON。
- 事务构建专项 3 项测试、改动范围静态检查、格式和空白检查均通过；工具提交 `03f4554`（`功能：事务构建支持 Dart 自动能力 / feat: support Dart auto capabilities in transactional builds`）。

## 模式一完整示例纳入 Dart 自动能力插入

- README 已把 50%、seed `0` 的 `--auto-dart-capabilities` 命令从可选“额外”步骤改为模式一完整流程的必经第二步。
- 命令仍以 `huanxin_product_deisoekdi_output` 为项目副本，目标为 `lib,packages/aixi_foundation/lib`；不改变 CLI 行为，也不触碰任何已有输出副本。
- 已通过 Markdown/参数路径交叉检查与 `git diff --check`；工具文档提交 `ae5bdbb`（`文档：将 Dart 自动能力插入纳入完整示例 / docs: include Dart auto capabilities in complete example`）。

## Manager/Logic/Widget Capability 模板

- 状态：已完成（100%）。
- Manager：`operation_context`、`lifecycle_snapshot`。
- Logic：`decision_context`、`rule_validation`。
- Widget：`presentation_metadata`、`accessibility_context`。
- 六个模板均为无状态本地计算，已接入全量建议、百分比选择、真实调用与定义生成。
- `junk_code` 目录在 Capability Analyzer 发现阶段完全排除，不出现在建议、候选、selection 或代码插入中。
- 环信默认全量文件已刷新：排除 `junk_code` 后为 1446 个业务 Class、464 个候选；Manager 31、Logic 3、Widget 306 个候选，三类均不再出现 `no_capability_suggestions`。
- 新 SHA-256：`1d81618d8516e2ad2916214ed07fbd81b46c98eee10682cc385f96c17636515f`；报告中 `junk_code` 路径数量为 0。
- 专项 11 项、完整 74 项通过；Analyzer 无 error/warning（45 条 info）。
- 工具提交：`e2523b3`（`功能：增加管理逻辑组件能力模板 / feat: add manager logic widget capabilities`）。

## 环信输出副本 50% Dart 差异化执行

- 在 `huanxin_all_output` 的 `lib,packages/aixi_foundation/lib` 范围执行 50%、seed `0` 自动插入；原始环信源码未修改。
- 464 个候选中选择并插入 232 项，分布于 207 个文件；selection 保存于输出副本 `reports/dart-capability-selection.json`。
- selected 文件 parser/formatter 检查通过，目标范围 Analyzer 未命中 selected 文件 error。
- 全仓 Flutter Analyzer 会扫描输出副本既有备份/示例/插件并报告大量历史 issue，不将其归因于本次插入。
- README 已补充 50% 自动插入完整示例、候选数量计算和 selection 复用说明；工具文档提交：`3350ef2`。

## iOS 第三方库随机接入与安全调用

- 状态：已完成（100%）。
- 已确认 10 个 CocoaPods 候选、每产品固定选择 3 个、`productId + capability seed` 确定性随机，以及启动后后台一次的无业务副作用调用策略。
- 已确认真实验证使用全新 `huanxin_third_party_output`，不覆盖现有输出。
- 已完成 schema v2、确定性选择、10 库审核池、本地 Pod/安全后台调用生成、四层依赖报告和 restore 接线；专项测试通过。
- 环信 `config/dependencies.yaml` 与 `config/ios_library_pool.yaml` 已更新为本次产品配置，业务源码未修改。
- 全新 `huanxin_third_party_output` 已完成 pod install、无签名 Release 和 unsigned IPA；Release Inspector 验收为 `selected=declared=locked=called=linked=3`，最终状态 `passed`。
- 修复真实构建暴露的 Pigeon Swift target 接线：生成 API/handler/installer 统一进入 `NativeCapabilities` Pod，并显式依赖 Flutter；失败输出保留在独立诊断目录。
- 完整 80 项测试通过；工具提交 `ecee52f`，提交信息为“实现 iOS 第三方库确定性接入 / Implement deterministic iOS third-party integration”。
- 修正工具 README 的模式一完整示例：默认流程仅使用 CLI 参数生成产品 Pod；五份 capability 配置仅作为随机第三方库/Native 能力流水线的可选追加项。

## 全项目状态盘点与文档校准

- 重读协作入口、工程总览、工作计划、工作进度、日报、工具 README、CI workflow 和详细设计；根仓库仅保留用户已有未跟踪 `tbr-shell-cladding`，工具工作树干净，原始环信的 staged capability 配置与 reports 未触碰。
- 重新运行工具基线：`fvm dart test -r expanded` 80 项通过；`fvm dart analyze` 无 error/warning，保留 39 条既有 info；只读格式检查报告 9 个待格式化文件，已登记为 WP-11 本地质量待办。
- 文档已统一：基础模式一/模式二和产品 Pod 不依赖五份 manifest；manifest 流水线仅为可选能力。详细设计新增实施状态快照，避免把实施前差距误读为当前缺口。
- 未完成项之一是 WP-11 的私有 CI 正式门禁：两份 Product Profile、签名凭据和 Release fixture 尚未提供，因此无法执行双 Profile 签名 Release。盘点时不存在 `huanxin_*_output`，历史无签名 IPA 记录不能替代当前可复查产物。
- 补充核对原始环信 manifest：Dart `rules`/`integrations` 为空，真实流水线的 Dart ready/reachable 为 0；这是为了不插入虚假业务调用，但仍是完整 Release Capability 目标的未完成项，需源码所有者确认真实调用锚点后实施。
- 交付：工具 README 代码围栏修正提交 `007851c`；全项目状态盘点与文档校准提交 `4c05044`；真实环信 Dart Capability 接线缺口补充提交 `aeb080e`。
